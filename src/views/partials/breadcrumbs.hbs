<nav class="text-sm text-text-300 mb-6">
 {{#each crumbs}}
  {{#if @last}}
   <span class="text-text-100 font-medium">{{label}}</span>
  {{else}}
   <a href="{{href}}" class="hover:text-ember-400">{{label}}</a>
   <span class="mx-1">/</span>
  {{/if}}
 {{/each}}
</nav>
