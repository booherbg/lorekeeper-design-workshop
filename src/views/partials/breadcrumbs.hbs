<nav class="text-sm text-parchment-300 mb-6">
  {{#each crumbs}}
    {{#if @last}}
      <span class="text-ink-800 font-medium">{{label}}</span>
    {{else}}
      <a href="{{href}}" class="hover:text-ember-500">{{label}}</a>
      <span class="mx-1">/</span>
    {{/if}}
  {{/each}}
</nav>
